{
  wayland.windowManager.hyprland.settings.exec-once = [ # 在 Hyprland 启动时执行一次的命令
    # "waybar"
    #"ags run&"
    # "ags -b hypr"
    "hyprpanel &"
    "fcitx5 --replace -d"
    "clash-verge"
  ];
}
